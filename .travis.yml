language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t kandruch/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kandruch/docker-react npm run test